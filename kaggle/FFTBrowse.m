classdef FFTBrowse % this is just a function for exploration purpose, kind of like a command which holds no data

    methods
        function show(kaggle_data_capsule) % this is generic name to do whatever the name suggests
            % just plot the fft of all like 
            
        end
    end
end